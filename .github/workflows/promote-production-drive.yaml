name: Promote google-drive to production

permissions:
  packages: write

on:
  workflow_dispatch:

jobs:
  promote-production:
    runs-on: ubuntu-latest
    concurrency:
      group: google-drive-promote-production
      cancel-in-progress: true
    steps:
      - name: Install crane
        uses: imjasonh/setup-crane@v0.4

      - name: Log in to GitHub Container Registry
        run: crane auth login ghcr.io -u "${{ github.actor }}" -p "${{ secrets.GITHUB_TOKEN }}"

      - name: Promote main image to latest
        env:
          IMAGE: ghcr.io/${{ github.repository }}/google-drive
        run: crane tag "${IMAGE}:main" latest
