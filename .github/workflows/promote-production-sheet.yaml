name: Promote google-sheets to production

permissions:
  packages: write

on:
  workflow_dispatch:

jobs:
  promote-production:
    runs-on: ubuntu-latest
    concurrency:
      group: google-sheets-promote-production
      cancel-in-progress: true
    steps:
      - name: Install crane
        uses: imjasonh/setup-crane@v0.4

      - name: Log in to GitHub Container Registry
        run: crane auth login ghcr.io -u "${{ github.actor }}" -p "${{ secrets.GITHUB_TOKEN }}"

      - name: Promote main image to latest
        env:
          IMAGE: ghcr.io/${{ github.repository }}/google-sheets
        run: crane tag "${IMAGE}:main" latest
